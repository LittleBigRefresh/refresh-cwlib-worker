# Refresh CwLib Worker

This project integrates with [Refresh](https://github.com/LittleBigRefresh/Refresh)'s database to allow Refresh to perform tasks and migrations that require CwLib.

The worker implementation is mostly the same as the `Refresh.Workers` in Refresh, ported to Java.

This project is currently work-in-progress and doesn't do much yet.